# Cpp_Power4
 Puissance 4 en C++

##  

Make sure the `obj/` directory exists and that `make` is installed on your system.

To build the project, juste use the make command:
```bash
make
```

This will create the `Puissance4` file that you can launch in a terminal.
```bash
./Puissance4
```
